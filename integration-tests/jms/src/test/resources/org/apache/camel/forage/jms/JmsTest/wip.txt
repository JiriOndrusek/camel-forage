artemis local with docker:

docker run -it --rm \
  -p 61616:61616 \
  -p 8161:8161 \
  -e ARTEMIS_USERNAME=artemis \
  -e ARTEMIS_PASSWORD=artemis \
  apache/activemq-artemis:latest

  ibmmq local with docker

  docker run -it --rm \
    -p 61616:61616 \
    -p 8161:8161 \
    -e ARTEMIS_USERNAME=artemis \
    -e ARTEMIS_PASSWORD=artemis \
    apache/activemq-artemis:latest